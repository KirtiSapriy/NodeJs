<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" value="<%=data.viewport%>" content="width=device-width, initial-scale=1.0">
    <title>Add Movie</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body class="bg-gray-900 text-white min-h-screen">

    <%-include('header')%>


        <main class="p-6 flex justify-center items-center">
            <div class="bg-gray-800 p-8 rounded-xl shadow-lg w-full max-w-md mt-10">
                <h2 class="text-2xl font-bold text-yellow-400 mb-6 text-center">➕ Update Movie</h2>


                <form action="/updateMovieData" method="post" enctype="multipart/form-data" class="space-y-5">

                    <input type="text" value="<%=data.id%>" name="id">
                    <div>
                        <label for="poster" class="block mb-1 font-semibold">Poster Image</label>
                        <input type="file" id="poster" name="poster" value="<%=data.poster%>"
                            class="w-full px-4 py-2 rounded-md bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400" />
                    </div>
                    <div>
                        <label for="title" class="block mb-1 font-semibold">Title</label>
                        <input type="text" id="title" name="title" value="<%=data.title%>"
                            class="w-full px-4 py-2 rounded-md bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400"
                            placeholder="Enter movie title" />
                    </div>

                    <div>
                        <label for="genre" class="block mb-1 font-semibold">Genre</label>
                        <input type="text" id="genre" name="genre" value="<%=data.genre%>"
                            class="w-full px-4 py-2 rounded-md bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400"
                            placeholder="e.g., Action, Drama" />
                    </div>

                    <div>
                        <label for="year" class="block mb-1 font-semibold">Release Year</label>
                        <input type="number" id="year" name="year" value="<%=data.year%>"
                            class="w-full px-4 py-2 rounded-md bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400"
                            placeholder="e.g., 2024" />
                    </div>


                    <div>
                        <button type="submit"
                            class="w-full bg-yellow-400 text-gray-900 font-semibold py-2 rounded-md hover:bg-yellow-300">
                            Update Movie
                        </button>
                    </div>
                </form>
            </div>
        </main>

</body>

</html>